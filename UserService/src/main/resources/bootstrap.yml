spring:
  application:
    name: UserService
  profiles:
    active: dev
  cloud:
    nacos:
#      config:
#        server-addr: localhost:8848
#        file-extension: yaml
      discovery:
        server-addr: localhost:8848